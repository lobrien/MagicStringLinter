on: push
name: Lint markdown
jobs:
  my-job:
    name: My job
    runs-on: ubuntu-latest  
    steps:
    - name: My step name
      run: |
        import os 

        print('Hello Github actions world')
        print('Second line')
        print(os.environ['PYTHONPATH'])
      shell: python
      